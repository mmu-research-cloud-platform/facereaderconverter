% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convertFRExcelFiles.R
\name{convertFRExcelFiles}
\alias{convertFRExcelFiles}
\title{Load FaceReader Excel exports}
\usage{
convertFRExcelFiles(
  inpath,
  return_data = TRUE,
  values_as_numeric = TRUE,
  clean_names = TRUE,
  fail_codes = FALSE,
  duplicate_timecodes_as_error = TRUE,
  sheet = 1,
  ...
)
}
\arguments{
\item{inpath}{Path to an existing \code{.xlsx} file.}

\item{return_data}{Return the parsed data instead of metadata.}

\item{values_as_numeric}{Convert \verb{Video Time} to \code{hms} and detailed values to
numeric.}

\item{clean_names}{Apply \code{janitor::clean_names()} to the data.}

\item{fail_codes}{Add a \code{fail_code} column for detailed exports.}

\item{duplicate_timecodes_as_error}{Throw an error if duplicate timecodes are
found.}

\item{sheet}{Excel sheet to read. Defaults to the first sheet.}

\item{...}{Additional arguments passed to \code{janitor::clean_names()}.}
}
\value{
Invisibly returns the parsed data when \code{return_data = TRUE};
otherwise returns metadata about the import.
}
\description{
Reads a FaceReader \code{.xlsx} file, detects the header row automatically,
and returns the parsed data frame. The function follows the same
post-processing conventions as \code{convertFRFiles()} where applicable.
}
\examples{
\dontrun{
convertFRExcelFiles(
  inpath = "FaceReaderOutput.xlsx",
  return_data = TRUE,
  values_as_numeric = TRUE
)
}

}
