% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_id_subject_metadata.R
\name{extract_subject_id_metadata}
\alias{extract_subject_id_metadata}
\title{Extract subject and ID metadata from a FaceReader filename}
\usage{
extract_subject_id_metadata(
  path,
  id_pattern = "(?<![0-9])\\\\s*[0-9]{4}\\\\s*(?![0-9])",
  subject_pattern = "(?<![a-z])(mum|teen)(?![a-z])"
)
}
\arguments{
\item{path}{Path to a FaceReader file.}

\item{id_pattern}{Regular expression used to extract the identifier. Defaults
to a four-digit identifier with optional surrounding whitespace.}

\item{subject_pattern}{Regular expression used to extract the subject. Defaults
to \code{mum} or \code{teen} as a standalone word.}
}
\value{
A list with character elements \code{id} and \code{subject}. Each element is
\code{NA} when its pattern does not match.
}
\description{
Extracts an identifier and subject label from a file path using caller-supplied
regular expressions. The identifier is trimmed after extraction, while the
subject is matched case-insensitively.
}
\examples{
\dontrun{
extract_subject_id_metadata(
  "8895 mum FR9_00024 mum_Analysis_detailed.xlsx",
  id_pattern = "(?<![0-9])\\\\s*[0-9]{4}\\\\s*(?![0-9])",
  subject_pattern = "(?<![a-z])(mum|teen)(?![a-z])"
)
}
}
