% Generated by hand to match the current source.
\name{synchrony}
\alias{synchrony}
\title{Calculate synchrony from converted episodes}
\usage{
synchrony(
  coded_data,
  subject_names = NULL,
  missing_threshold = 0,
  exclude_emotions = "neutral"
)
}
\arguments{
\item{coded_data}{Output from \code{convert_to_episodes()}.}

\item{subject_names}{Character vector of subject labels to compare. If \code{NULL}, all unique subjects in \code{coded_data$coding} are used. If supplied, the data are filtered to those subject levels before synchrony is calculated.}

\item{missing_threshold}{Numeric scalar in \code{[0, 1]}. Denominator episodes are dropped when the comparison subject is missing for more than this proportion of frames within the episode.}

\item{exclude_emotions}{Character vector of emotions to exclude from the denominator calculation. Default is \code{"neutral"}.}
}
\value{
A data.table with columns \code{id}, \code{denominator}, \code{numerator}, \code{emotion}, \code{n_episodes}, and \code{synchrony}.
}
\description{
Calculate synchrony from converted episodes.
}
