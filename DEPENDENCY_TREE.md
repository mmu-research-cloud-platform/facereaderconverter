# R Function Dependency Tree

_Generated from static source inspection; direct dependencies only._

| Function | Depends on | Depended on by | Required structure and inputs |
|---|---|---|---|
| `add_delta_column()` | `all_deltas()`<br>`is_fr_coding()`<br>`parse_time_to_frame()`<br>`resolve_fr_metadata()` | `delta()` | Signature: `add_delta_column(coding, delta = 0.1, delta_window = 0.2, fps = 30L, cores = 0L)`; table-like coding with `id`, `subject`, `emotion`, and `frame` or `video_time` plus values, or an `fr_coding` object |
| `convertFRDirectory()` | `convertFRFiles()`<br>`fr_output_path()`<br>`loadFRfile()`<br>`map_paths()`<br>`resolve_conversion_metadata()` | — | Signature: `convertFRDirectory(inpath, outpath = inpath, recursive = TRUE, pattern = NULL, values_as_numeric = TRUE, clean_names = TRUE, save_metadata = outpath, metadata_filename = "metadata.csv", fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, id = NULL, subject = NULL, cores = 0L, ...)`; input/output directories and file-selection, metadata, and processing controls |
| `convertFRExcelFiles()` | `add_fr_metadata()`<br>`check_duplicate_timecodes()`<br>`detect_fr_header_row()`<br>`resolve_conversion_metadata()` | `loadFRfile()` | Signature: `convertFRExcelFiles(inpath, return_data = TRUE, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, sheet = 1, id = NULL, subject = NULL, ...)`; existing `.xlsx` path, sheet selector, and import/metadata controls |
| `convertFRFiles()` | `add_fr_metadata()`<br>`check_duplicate_timecodes()`<br>`detect_fr_header_row()`<br>`fr_output_path()`<br>`resolve_conversion_metadata()` | `convertFRDirectory()`<br>`loadFRfile()` | Signature: `convertFRFiles(inpath, outpath = paste0(tools::file_path_sans_ext(inpath), ".csv"), return_data = FALSE, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, id = NULL, subject = NULL, ...)`; existing `.txt` path, output path, and import/metadata controls |
| `convert_to_episodes()` | `all_deltas()`<br>`fr_coding()`<br>`hysteresis_state()`<br>`max_delta_ranges()`<br>`parse_time_to_frame()` | — | Signature: `convert_to_episodes(coding_df, T_up = 0.20, T_down = 0.1, delta = 0.10, delta_window = 0.2, min_dur_sec = 0.1, consecutive_missing = 150L, fps = 30L, cores = 0L)`; coding data with identifiers, emotion/value data, and `frame` or `video_time`; threshold and timing controls |
| `delta()` | `add_delta_column()` | — | Signature: `delta(coding, delta = 0.1, delta_window = 0.2, fps = 30L, cores = 0L)`; same coding structure and scalar controls as `add_delta_column()`; deprecated wrapper |
| `delta_episodes()` | `get_fr_metadata()`<br>`is_fr_coding()`<br>`resolve_fr_metadata()` | — | Signature: `delta_episodes(coding, fps = 30L, cores = 0L)`; coding table or `fr_coding` object with value, delta, identifier, emotion, and time/frame fields |
| `export_shared_synchrony_clips()` | `prepare_shared_synchrony_clips()` | — | Signature: `export_shared_synchrony_clips(coded_data, shared_synchrony, video_paths, n = 10L, emotion = "happy", optimised_subject = "both", only_synchronies = TRUE, buffer = 0, buffer_units = c("seconds", "frames"), buffer_frames = 0L, buffer_seconds = 0, output_path = NULL, output = c("zip", "folder"), overwrite = FALSE, ffmpeg = "ffmpeg")`; `fr_coding` metadata, shared-synchrony table, named video paths, and output controls |
| `extract_id_subject_metadata()` | `extract_subject_id_metadata()` | — | Signature: `extract_id_subject_metadata(path, id_pattern = ..., subject_pattern = ...)`; file path and optional regular-expression patterns; compatibility alias |
| `extract_subject_id_metadata()` | — | `extract_id_subject_metadata()` | Signature: `extract_subject_id_metadata(path, id_pattern = ..., subject_pattern = ...)`; file path and optional regular-expression patterns; returns `id` and `subject` character elements |
| `loadFRfile()` | `add_fr_metadata()`<br>`convertFRExcelFiles()`<br>`convertFRFiles()`<br>`resolve_conversion_metadata()` | — | Signature: `loadFRfile(inpath, values_as_numeric = TRUE, clean_names = TRUE, fail_codes = FALSE, duplicate_timecodes_as_error = TRUE, sheet = 1, id = NULL, subject = NULL, ...)`; existing `.txt`, `.xlsx`, or `.csv` path and import/metadata controls |
| `locf()` | `fr_coding()`<br>`is_fr_coding()`<br>`resolve_fr_metadata()` | — | Signature: `locf(coding, fps = 30, consecutive_missing = NULL)`; coding table or `fr_coding` object with identifier, emotion, time/value, run, and state fields |
| `map_paths()` | — | `convertFRDirectory()` | Signature: `map_paths(input_dir, output_dir, files)`; character input/output roots and file paths |
| `negative_controls()` | `prepare_synchrony_inputs()`<br>`synchrony_by_episode()` | — | Signature: `negative_controls(coded_data, episodes, mutually_exclusive = FALSE, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral", max_tries = 1000L)`; converted coding data and episode input with synchrony constraints |
| `parse_time_to_frame()` | — | `add_delta_column()`<br>`convert_to_episodes()`<br>`prepare_synchrony_inputs()` | Signature: `parse_time_to_frame(video_time, fps)`; character time values or numeric time/frame values and numeric frames-per-second |
| `reaction_rate()` | `build_reaction_rate_episode_table()`<br>`prepare_reaction_rate_inputs()` | — | Signature: `reaction_rate(coded_data, time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0)`; converted coding object or validated coding table and window/filter controls |
| `reaction_rate_by_episode()` | `build_reaction_rate_episode_table()`<br>`prepare_reaction_rate_inputs()` | — | Signature: `reaction_rate_by_episode(coded_data, time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0)`; same input structure and validation controls as `reaction_rate()` |
| `shared_synchronous_episodes()` | `attach_fr_metadata()`<br>`build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `shared_synchronous_episodes(coded_data, episodes = NULL, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; converted coding object with episode and identifier/state fields |
| `synchrony()` | `build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | — | Signature: `synchrony(coded_data, episodes = NULL, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; converted coding object and optional compatible episode ranges |
| `synchrony_by_episode()` | `attach_fr_metadata()`<br>`build_synchrony_episode_table()`<br>`prepare_synchrony_inputs()` | `negative_controls()` | Signature: `synchrony_by_episode(coded_data, episodes = NULL, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; converted coding object and optional episode ranges |
| `to_seconds()` | — | — | Signature: `to_seconds(x, digits = 3L)`; character values in supported time formats and non-negative numeric precision |

## Internal and helper functions

| Function | Depends on | Depended on by | Required structure and inputs |
|---|---|---|---|
| `add_fr_metadata()` | — | `convertFRExcelFiles()`<br>`convertFRFiles()`<br>`loadFRfile()` | Signature: `add_fr_metadata(data, metadata)`; table-like data and metadata list |
| `all_deltas()` | — | `add_delta_column()`<br>`convert_to_episodes()` | Signature: `all_deltas(value, k, delta)`; numeric values, integer window, and numeric threshold; generated native wrapper |
| `attach_fr_metadata()` | — | `prepare_reaction_rate_inputs()`<br>`prepare_synchrony_inputs()`<br>`shared_synchronous_episodes()`<br>`synchrony_by_episode()` | Signature: `attach_fr_metadata(x, metadata)`; object and metadata list |
| `build_reaction_rate_episode_table()` | — | `reaction_rate()`<br>`reaction_rate_by_episode()` | Signature: `build_reaction_rate_episode_table(inputs)`; validated reaction-rate input list |
| `build_synchrony_episode_table()` | — | `shared_synchronous_episodes()`<br>`synchrony()`<br>`synchrony_by_episode()` | Signature: `build_synchrony_episode_table(inputs)`; validated synchrony input list |
| `check_duplicate_timecodes()` | — | `convertFRExcelFiles()`<br>`convertFRFiles()` | Signature: `check_duplicate_timecodes(df, duplicate_timecodes_as_error = TRUE)`; imported FaceReader table with `Video Time` and optional identifiers |
| `detect_fr_header_row()` | — | `convertFRExcelFiles()`<br>`convertFRFiles()` | Signature: `detect_fr_header_row(lines)`; character lines containing a FaceReader header |
| `fr_coding()` | — | `convert_to_episodes()` | Signature: `fr_coding(coding, episodes = NULL, deltas = NULL, metadata = list())`; coding table and optional episodes, deltas, and metadata |
| `fr_output_path()` | — | `convertFRDirectory()`<br>`convertFRFiles()` | Signature: `fr_output_path(outpath, metadata)`; output path and metadata list |
| `get_fr_metadata()` | `is_fr_coding()` | `delta_episodes()`<br>`prepare_reaction_rate_inputs()`<br>`prepare_synchrony_inputs()`<br>`resolve_fr_metadata()` | Signature: `get_fr_metadata(x)`; `fr_coding`-compatible list object |
| `hysteresis_state()` | — | `convert_to_episodes()` | Signature: `hysteresis_state(v, k, T_up, T_down, delta, min_len, consecutive_missing)`; numeric values and episode threshold/window controls; generated native wrapper |
| `is_fr_coding()` | — | `add_delta_column()`<br>`delta_episodes()`<br>`get_fr_metadata()`<br>`locf()`<br>`prepare_reaction_rate_inputs()`<br>`prepare_synchrony_inputs()` | Signature: `is_fr_coding(x)`; list with `coding` and `episodes` elements |
| `is_reaction_rate_scalar()` | — | `is_reaction_rate_whole()`<br>`prepare_reaction_rate_inputs()` | Signature: `is_reaction_rate_scalar(x)`; length-one non-missing value |
| `is_reaction_rate_whole()` | `is_reaction_rate_scalar()` | `prepare_reaction_rate_inputs()` | Signature: `is_reaction_rate_whole(x)`; numeric scalar satisfying the package whole-number tolerance |
| `max_delta_ranges()` | — | `convert_to_episodes()` | Signature: `max_delta_ranges(value, start_row, end_row, group_start_row, k)`; numeric values, inclusive ranges, group starts, and integer window; generated native wrapper |
| `prepare_reaction_rate_inputs()` | `attach_fr_metadata()`<br>`get_fr_metadata()`<br>`is_fr_coding()`<br>`is_reaction_rate_scalar()`<br>`is_reaction_rate_whole()`<br>`resolve_fr_metadata()` | `reaction_rate()`<br>`reaction_rate_by_episode()` | Signature: `prepare_reaction_rate_inputs(coded_data, time_limit = 3, time_limit_frames = NULL, exclude_start = 0.1, exclude_start_frames = NULL, fps = 30L, subject_names = NULL, exclude_emotions = "neutral", minimum_threshold = 0, constraint_method = "episode")`; converted coding object or coding table with coding/episode/delta fields and validated controls |
| `prepare_shared_synchrony_clips()` | — | `export_shared_synchrony_clips()` | Signature: `prepare_shared_synchrony_clips(coded_data, shared_synchrony, video_paths, n, emotion, optimised_subject = "both", only_synchronies = TRUE, buffer = 0, buffer_units = c("seconds", "frames"), buffer_frames = 0L, buffer_seconds = 0)`; coding metadata, synchrony intervals, named video paths, and buffer controls |
| `prepare_synchrony_inputs()` | `attach_fr_metadata()`<br>`get_fr_metadata()`<br>`parse_time_to_frame()`<br>`resolve_fr_metadata()` | `negative_controls()`<br>`shared_synchronous_episodes()`<br>`synchrony()`<br>`synchrony_by_episode()` | Signature: `prepare_synchrony_inputs(coded_data, episodes = NULL, subject = "subject", id = "id", time_limit = 3, time_limit_frames = NULL, constraint_method = "episode", fps = 30L, missing_threshold = 0, exclude_emotions = "neutral")`; converted coding object or compatible coding/episode tables and synchrony controls |
| `resolve_conversion_metadata()` | — | `convertFRDirectory()`<br>`convertFRExcelFiles()`<br>`convertFRFiles()`<br>`loadFRfile()` | Signature: `resolve_conversion_metadata(id, subject, inpath)`; optional scalar metadata values or functions of an input path |
| `resolve_fr_metadata()` | `get_fr_metadata()` | `add_delta_column()`<br>`delta_episodes()`<br>`locf()`<br>`prepare_reaction_rate_inputs()`<br>`prepare_synchrony_inputs()` | Signature: `resolve_fr_metadata(value, x, name, default = value)`; metadata value, `fr_coding`-compatible object, metadata name, and default |
| `update_fr_metadata()` | `get_fr_metadata()` | — | Signature: `update_fr_metadata(x, metadata = list())`; object and metadata list |

## Notes

- Export status is authoritative from `NAMESPACE`: 20 exported functions are listed in the first table and 22 internal/helper functions in the second table.
- Both tables use the complete project-function graph. Dependency cells include internal/helper and exported functions; base R and third-party package calls are excluded.
- `extract_subject_id_metadata()` is the requested exported name; `extract_id_subject_metadata()` remains an exported compatibility alias.
- `all_deltas()`, `hysteresis_state()`, and `max_delta_ranges()` are generated Rcpp wrappers in `R/RcppExports.R`; their native implementations were not inspected as R functions.
- Functions defined locally inside top-level functions are not package-level project functions and are not listed as rows or definite graph dependencies.
- Calls through dynamic dispatch, non-standard evaluation, strings, native `.Call()` implementations, and runtime-generated objects were not treated as definite dependencies.
- Unresolved or ambiguous calls: `everything()` in `to_seconds()` is unqualified and not imported in `NAMESPACE`; `setDT()` in `delta_episodes()` is unqualified but `data.table` is imported. Native `.Call()` implementations remain outside R-only static inspection.
- No duplicate top-level function definitions were found.
